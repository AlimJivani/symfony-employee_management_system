<!-- Image and text -->
<nav class="navbar navbar-white bg-light">
    
    <a class="navbar-brand" href="{{ path('dashboard') }}">
        <img src="{{ asset('/img/Capture.PNG') }}" width="80" height="35" class="d-inline-block align-top"
            alt="">
        <span class="text-dark"> <b>Karavya</b> Solutions</span>
    </a>

<ul class="nav">
        {% if app.user %}
       <li class="nav-item mt-2">
        <b><small>  Greeting!  {{ app.user.email}}, You are here as {{ app.user.roles[0] }}</small></b>
        </li>
       <li>
            <a class="nav-link" href="{{ path('dashboard') }}">Dashboard </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
        </li>
        {% endif %}

        {% if not app.user %}
         <li class="nav-item">
            <a class="nav-link active" href="{{ path('app_login') }}">Login</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_signup') }}">Signup</a>
        </li>
        {% endif %}
        {# <li class="nav-item">
            <a class="nav-link" href="{{ path('app_signup') }}"><i class="fa fa-cog fa-lg" aria-hidden="true"></i></a>
        </li> #}

</ul>
</nav>

